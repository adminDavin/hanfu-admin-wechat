<view class="banner-box-three">
  <view bind:tap="onClickRanking" wx:for="{{rankingData}}" wx:key="index" data-item="{{item}}">
    <view><text>{{item.seniorityName}}</text></view>
    <image src="{{item.imageUrl}}" style="width:100%"></image>
  </view>
</view>
<view>
  <product-list scrollH="{{scrollH}}" imgWidth="{{imgWidth}}"></product-list>
</view>
